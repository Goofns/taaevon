# Normalize line endings: store text as LF in the repo, regardless of OS.
# Keeps Windows working copies from churning CRLF into diffs and matches the
# Linux CI runner.
* text=auto eol=lf

# Binary assets — never normalize.
*.png binary
*.jpg binary
*.jpeg binary
*.gif binary
*.ico binary
*.ttf binary
*.otf binary
*.db binary
